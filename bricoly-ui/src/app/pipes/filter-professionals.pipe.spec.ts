import { FilterProfessionalsPipe } from './filter-professionals.pipe';

describe('FilterProfessionalsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfessionalsPipe();
    expect(pipe).toBeTruthy();
  });
});
